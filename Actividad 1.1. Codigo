function setup() {
  createCanvas(400, 400);
  
  //Buanas tardes o noches profa, le voy a estar poniendo unos comentarios para que usted pueda ver a que partecita del codigo pertenece cada figura, como las manos la boca, etc 
  
  // Color del fondo
  background(220);
  
  // Cabeza
  fill(180, 180, 190);
  stroke(0);
  strokeWeight(3);
  rect(100, 70, 200, 170);
  
  // Ojos
  fill(50, 200, 255);
  stroke(0);
  strokeWeight(3);
  ellipse(155, 125, 45, 45);
  
  fill(50, 200, 255);
  ellipse(245, 125, 45, 45);
  
  // Boca
  fill(80, 80, 80);
  rect(155, 175, 90, 30);
  
  // Antena
  stroke(0);
  strokeWeight(4);
  line(200, 70, 200, 35);
  
  // Parte superior de la antena
  fill(255, 80, 80);
  stroke(0);
  strokeWeight(2);
  ellipse(200, 25, 20, 20);
  
  // Cuerpo
  fill(120, 150, 180);
  stroke(0);
  strokeWeight(3);
  rect(125, 240, 150, 100);
  
  // Botón del cuerpo
  fill(255, 200, 50);
  ellipse(200, 290, 35, 35);
  
  // Brazos
  stroke(0);
  strokeWeight(6);
  line(125, 260, 75, 310);
  line(275, 260, 325, 310);
  
  // Piernas
  stroke(0);
  strokeWeight(7);
  line(165, 340, 150, 380);
  line(235, 340, 250, 380);
  
  // Triángulo decorativo
  fill(255, 100, 100);
  stroke(0);
  strokeWeight(2);
  triangle(185, 215, 200, 195, 215, 215);
}
